- name: Make sure the target dirs exist
  file:
    path: '{{ images_directory }}/raw.xz'
    group: 'kvm'
    mode: '775'
    state: directory

- name: Copy Images
  copy:
    src: 'images/raw.xz/{{ item.image_filename }}'
    dest: '{{ images_directory }}/raw.xz/{{ item.image_filename }}'
    owner: '{{ kvm_user }}'
    group: kvm
  loop: '{{ guest_os_images }}'
